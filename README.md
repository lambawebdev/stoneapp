## Для тестирования:

php artisan test --filter=testCreateNewUser

1. UserRegistered вызывается в контроллере, сразу после создания пользователя (можно привязать на ивент или на observer)
Можно вызвать в UserService, думаю, в данном примере это не играет большого значения.
2. Создание пользователя происходит напрямую в сервисе + логирование в файл после создания
3. Присвоение ролей/пермишнов в идеале должен выполнять отдельный функционал (не завязанный на создание пользователя), 
можно использовать spatie permissions
